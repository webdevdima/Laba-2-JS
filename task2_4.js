function slice(array, fn, initial) {
    let i = 0;
    let result = initial || array[i++];
    for (let i = 0; i < array.length; i++) {
        results.push(array[i]);
    }
    fn[a, b] = result[i]
    return fn;
}